---
phase: 02-asset-localization-route-restructuring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/i18n/de.json.jsx (renamed to de.js)
  - src/components/i18n/en.json.jsx (renamed to en.js)
  - src/components/i18n/fr.json.jsx (renamed to fr.js)
  - src/components/i18n/it.json.jsx (renamed to it.js)
autonomous: true
requirements: [I18N-01, I18N-02]
user_setup: []

must_haves:
  truths:
    - "No .json.jsx files remain in src/components/i18n/"
    - "If replacement .js files are created, they exist and are importable"
    - "No import paths reference .json.jsx anywhere in source code"
    - "Build succeeds without errors"
  artifacts:
    - path: "src/components/i18n/translations.jsx"
      provides: "All translation data (unchanged — already has all translations inline)"
  key_links:
    - from: "src/components/useTranslations.jsx"
      to: "src/components/i18n/translations.jsx"
      via: "Named imports { en, de, it, fr }"
      pattern: "from.*i18n/translations"
---

<objective>
Rename .json.jsx i18n files to .js extension and verify no import paths reference the old names.

Purpose: Remove the misleading .json.jsx extension from translation files, aligning with the rest of the .jsx codebase.
Output: Renamed (or removed) i18n files with updated imports if needed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asset-localization-route-restructuring/02-CONTEXT.md

@src/components/i18n/translations.jsx
@src/components/i18n/de.json.jsx
@src/components/i18n/en.json.jsx
@src/components/i18n/fr.json.jsx
@src/components/i18n/it.json.jsx
@src/components/useTranslations.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename .json.jsx files to .js and update any imports</name>
  <files>src/components/i18n/de.json.jsx, src/components/i18n/en.json.jsx, src/components/i18n/fr.json.jsx, src/components/i18n/it.json.jsx</files>
  <action>
The four `.json.jsx` files (`de.json.jsx`, `en.json.jsx`, `fr.json.jsx`, `it.json.jsx`) are currently empty stubs — all translation data lives in `translations.jsx` which exports `en`, `de`, `it`, `fr` objects inline.

Since the files are empty and unused:
1. Check if any file in `src/` imports from these `.json.jsx` files: `grep -r "json.jsx" src/` and `grep -r "de.json\|en.json\|fr.json\|it.json" src/`
2. If no imports reference them: **delete** all four `.json.jsx` files (they serve no purpose)
3. If any imports DO reference them: rename from `.json.jsx` to `.js` using `mv` commands, then update the import paths

Expected outcome: The files are unused (translations.jsx has everything; useTranslations.jsx imports from translations.jsx only), so deletion is the correct action. This satisfies I18N-01 (no misleading .json.jsx files remain) and I18N-02 (no import paths to update since files are unused).

After deletion, verify:
- `grep -r ".json.jsx" src/` returns zero results
- `useTranslations.jsx` still imports correctly from `./i18n/translations`
- Build succeeds
  </action>
  <verify>
grep -r ".json.jsx" src/ — zero results
ls src/components/i18n/ — should show only translations.jsx
npm run build — succeeds
  </verify>
  <done>All .json.jsx files removed from i18n directory; no import paths reference .json.jsx; build passes; translations.jsx remains the single source of truth for all translation data</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No `.json.jsx` files exist in `src/components/i18n/`
- [ ] `grep -r ".json.jsx" src/` returns zero results
- [ ] `grep -r "json\.jsx" src/` returns zero results
- [ ] `src/components/i18n/translations.jsx` still exists and exports all translations
- [ ] `npm run build` completes without errors
</verification>

<success_criteria>
- No .json.jsx files remain in the i18n directory
- No import paths reference .json.jsx anywhere in source
- Translations continue working correctly (translations.jsx unchanged)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-localization-route-restructuring/02-03-SUMMARY.md`
</output>
