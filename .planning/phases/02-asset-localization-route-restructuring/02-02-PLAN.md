---
phase: 02-asset-localization-route-restructuring
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/App.jsx
  - src/Layout.jsx
  - src/pages/Landing.jsx
  - src/pages/Products.jsx
  - src/pages/ProductDetail.jsx
  - src/utils/index.ts (renamed to index.js)
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03]
user_setup: []

must_haves:
  truths:
    - "App uses HashRouter — URLs contain # (e.g. http://localhost:5173/#/products)"
    - "Product detail pages use path segments: #/product/turntrack (not query params)"
    - "All navigation links work correctly with hash routing"
    - "Landing page is served at #/ only — no /landing alias"
    - "Unmatched routes show 404 NotFound page"
    - "createPageUrl() function no longer exists in source"
    - "Case-insensitive product slug lookup works (url 'turntrack' matches product)"
  artifacts:
    - path: "src/App.jsx"
      provides: "HashRouter with product/:slug routes"
      contains: "HashRouter"
    - path: "src/utils/index.js"
      provides: "Utility file without createPageUrl (or empty/removed)"
    - path: "src/pages/ProductDetail.jsx"
      provides: "Product detail using useParams() for slug instead of query params"
      contains: "useParams"
    - path: "src/Layout.jsx"
      provides: "Navigation with direct path strings, no createPageUrl"
    - path: "src/pages/Landing.jsx"
      provides: "Landing page links with direct paths, no createPageUrl"
    - path: "src/pages/Products.jsx"
      provides: "Product listing with /product/{slug} links"
  key_links:
    - from: "src/App.jsx"
      to: "src/pages/ProductDetail.jsx"
      via: "Route path /product/:slug"
      pattern: '/product/:slug'
    - from: "src/pages/Products.jsx"
      to: "/product/{id}"
      via: "Link to product detail"
      pattern: '/product/'
    - from: "src/pages/Landing.jsx"
      to: "/product/{slug}"
      via: "Link to product detail in seriesData"
      pattern: '/product/'
---

<objective>
Convert from BrowserRouter to HashRouter, restructure product detail routing from query params to path segments, and remove createPageUrl().

Purpose: Enable Apache/static-file-server compatible routing via hash URLs, and clean up the routing utility layer.
Output: HashRouter-based app with /product/:slug routes and direct path strings in all components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asset-localization-route-restructuring/02-CONTEXT.md
@.planning/phases/02-asset-localization-route-restructuring/02-01-SUMMARY.md

@src/App.jsx
@src/Layout.jsx
@src/pages/Landing.jsx
@src/pages/Products.jsx
@src/pages/ProductDetail.jsx
@src/utils/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert to HashRouter and add product/:slug route</name>
  <files>src/App.jsx, src/pages/ProductDetail.jsx</files>
  <action>
**src/App.jsx:**
1. Change import from `BrowserRouter` to `HashRouter` (from `react-router-dom`)
2. Replace `<BrowserRouter>` / `</BrowserRouter>` with `<HashRouter>` / `</HashRouter>`
3. Remove the `/landing` alias route (Landing is only at `/`)
4. Change `/productdetail` route to `/product/:slug` pointing to ProductDetail
5. Keep all other routes as-is: `/products`, `/calculator`, `/documentation`, `/impressum`, `/datenschutz`, `*` → NotFound

**src/pages/ProductDetail.jsx:**
1. Import `useParams` from `react-router-dom` (add to existing import)
2. Remove the `URLSearchParams` / `window.location.search` logic for getting `series`
3. Instead, get slug from route params: `const { slug } = useParams();`
4. Do case-insensitive product lookup: find product where `p.id.toLowerCase() === slug.toLowerCase()` (product IDs are already lowercase, but be defensive)
5. The rest of the component logic stays the same — it already handles missing products with a fallback
  </action>
  <verify>npm run build — succeeds without errors</verify>
  <done>App uses HashRouter; ProductDetail reads slug from URL path via useParams; /landing route removed; /product/:slug route exists</done>
</task>

<task type="auto">
  <name>Task 2: Replace createPageUrl with direct path strings everywhere</name>
  <files>src/Layout.jsx, src/pages/Landing.jsx, src/pages/Products.jsx, src/pages/ProductDetail.jsx, src/utils/index.ts</files>
  <action>
Remove `createPageUrl()` from all files and replace with direct path strings. Convert utils/index.ts to .js.

**src/utils/index.ts → src/utils/index.js:**
1. Rename file from `.ts` to `.js`
2. Remove the `createPageUrl` function entirely
3. If the file becomes empty, either delete it or leave it as an empty module. Since other files may import from `@/utils`, leave it as an empty `export {};` or remove the file AND all imports of it.

**src/Layout.jsx** — Remove `import { createPageUrl } from "@/utils"` and replace all usages:
- `createPageUrl("Landing")` → `"/"`
- `createPageUrl("Products")` → `"/products"`
- `createPageUrl("Documentation")` → `"/documentation"`
- `createPageUrl("Calculator")` → `"/calculator"`
- Any other createPageUrl calls → corresponding lowercase path

**src/pages/Landing.jsx** — Remove `import { createPageUrl } from "@/utils"` and replace:
- `createPageUrl("Products")` → `"/products"`
- `createPageUrl("Calculator")` → `"/calculator"`
- `createPageUrl(\`ProductDetail?series=${item.series}\`)` → `` `/product/${item.id}` `` (use item.id which is already lowercase)

**src/pages/Products.jsx** — Remove `import { createPageUrl } from "@/utils"` and replace:
- `createPageUrl(\`ProductDetail?series=${product.series}\`)` → `` `/product/${product.id}` ``

**src/pages/ProductDetail.jsx** — Remove `import { createPageUrl } from "@/utils"` and replace:
- `createPageUrl("Products")` → `"/products"`
- `createPageUrl("Calculator")` → `"/calculator"`
- `createPageUrl("Documentation")` → `"/documentation"`

IMPORTANT: Use `item.id` / `product.id` (lowercase) for product detail links, NOT `item.series` (mixed case). The IDs are already lowercase slugs matching the route.
  </action>
  <verify>
grep -rn "createPageUrl" src/ — zero results
grep -rn "index.ts" src/ — zero results (no .ts imports remaining)
npm run build — succeeds without errors
  </verify>
  <done>createPageUrl removed from all files; all links use direct path strings; utils/index.ts converted to .js or removed; build passes; no createPageUrl references remain</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "BrowserRouter" src/` returns zero results
- [ ] `grep -r "createPageUrl" src/` returns zero results
- [ ] `grep -r "productdetail" src/App.jsx` returns zero results (old route removed)
- [ ] `grep "product/:slug" src/App.jsx` returns the new route
- [ ] `grep "useParams" src/pages/ProductDetail.jsx` confirms slug-based routing
- [ ] `grep "window.location.search" src/` returns zero results
- [ ] `npm run build` completes without errors
</verification>

<success_criteria>
- App uses HashRouter (not BrowserRouter)
- Product detail route is /product/:slug (not /productdetail with query params)
- All createPageUrl() calls replaced with direct path strings
- Product links use lowercase IDs as slugs
- /landing alias route removed
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-localization-route-restructuring/02-02-SUMMARY.md`
</output>
