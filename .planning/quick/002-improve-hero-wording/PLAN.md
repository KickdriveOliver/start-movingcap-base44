# Plan: Improve hero wording

## Objective
Promote "MovingCap: Sophisticated Motion. Radically Simple." to the main headline (`hero_title`) in the hero section and rewrite the `hero_subtitle` and `hero_description` to better reflect the key technical strengths of the MovingCap product line (cabinet-free, integrated control, web interface, Python, absolute positioning), based on fullmo.de and movingcap.com.

## Steps
1. Create this plan.
2. Present the drafted wording proposals to the user for EN, DE, IT, and FR.
3. Replace the keys `hero_title`, `hero_subtitle`, and `hero_description` in `src/components/i18n/translations.jsx`.
4. Validate changes (locally visually/semantically).
5. Update state and write summary.