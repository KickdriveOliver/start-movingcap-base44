---
phase: 03-build-pwa-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.js
  - package.json
  - package-lock.json
  - index.html
  - src/components/i18n/translations.jsx
autonomous: true
requirements: [BUILD-01, BUILD-02]
user_setup: []

must_haves:
  truths:
    - "Vite config uses standard resolve.alias for @/ paths"
    - "Build output uses relative asset paths (base: './')"
    - "VITE_BASE_URL environment variable can override base path at build time"
    - "package.json name is 'movingcap-start' not 'base44-app'"
    - "No stale manifest.json link in index.html"
  artifacts:
    - path: "vite.config.js"
      provides: "Production-ready Vite config with configurable base"
      contains: "process.env.VITE_BASE_URL"
    - path: "package.json"
      provides: "Clean project identity"
      contains: "movingcap-start"
    - path: "index.html"
      provides: "Clean HTML entry without stale manifest link"
  key_links:
    - from: "vite.config.js"
      to: "dist/assets/"
      via: "Vite base config"
      pattern: "base.*VITE_BASE_URL"
---

<objective>
Finalize Vite config with configurable base path, relative asset paths, and clean up residual Base44 references.

Purpose: Make the build output portable — deployable to any subfolder or root path — and remove the last traces of the old platform identity.
Output: Updated vite.config.js, cleaned package.json, cleaned index.html.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-build-pwa-deployment/03-CONTEXT.md
@.planning/phases/03-build-pwa-deployment/03-RESEARCH.md

@vite.config.js
@package.json
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update vite.config.js with configurable base path</name>
  <files>vite.config.js</files>
  <action>
  Update `vite.config.js` to add `base: process.env.VITE_BASE_URL || './'` at the top level of the config.
  The `@/` alias is already configured and correct — keep it.
  The `react()` plugin is already present — keep it.
  Do NOT add vite-plugin-pwa yet (that's Plan 02).

  Result should look like:
  ```js
  import react from '@vitejs/plugin-react'
  import { defineConfig } from 'vite'
  import path from 'path'

  export default defineConfig({
    base: process.env.VITE_BASE_URL || './',
    plugins: [react()],
    resolve: {
      alias: {
        '@': path.resolve(__dirname, './src'),
      },
    },
  })
  ```
  </action>
  <verify>Run `npx vite build` and confirm build completes. Check that asset paths in `dist/index.html` are relative (start with `./assets/` not `/assets/`).</verify>
  <done>vite.config.js has `base: process.env.VITE_BASE_URL || './'` and build output uses relative paths</done>
</task>

<task type="auto">
  <name>Task 2: Clean up package.json and index.html</name>
  <files>package.json, index.html</files>
  <action>
  1. In `package.json`: Change `"name": "base44-app"` to `"name": "movingcap-start"`.

  2. In `index.html`: Remove the line `<link rel="manifest" href="/manifest.json" />` — there is no manifest.json file in the project, and vite-plugin-pwa will inject its own manifest link in Plan 02. Keep all other head contents.

  3. In `index.html`: Update the favicon link from `href="/favicon.svg"` to `href="favicon.svg"` (relative path, removing leading slash) so it works with the relative base path.

  4. In `index.html`: Update the script src from `src="/src/main.jsx"` to `src="./src/main.jsx"` — wait, this only matters in dev mode and Vite handles it. Actually, keep it as-is because Vite transforms this during build. Leave the script tag unchanged.
  </action>
  <verify>Run `npm run build` and verify `dist/index.html` does NOT contain `manifest.json`. Verify `package.json` name is `movingcap-start`. Run `npm run dev` to confirm dev server still works.</verify>
  <done>package.json name is `movingcap-start`, index.html has no stale manifest link, favicon uses relative path</done>
</task>

<task type="auto">
  <name>Task 3: Verify build output is portable</name>
  <files></files>
  <action>
  1. Run `npm run build` (standard build with `base: './'`).
  2. Inspect `dist/index.html` — all script/link tags should use relative paths (`./assets/...`).
  3. Run `npx vite preview` briefly to confirm it serves correctly.
  4. Verify: `grep -r "base44" src/` — only expect legitimate hits in `translations.jsx` (legal Impressum text mentioning the platform by name, which is factual content).
  5. Verify: `grep "base44" package.json` returns no results.
  </action>
  <verify>Build succeeds, dist/index.html uses relative paths, no base44 references in package.json</verify>
  <done>Build output is portable with relative paths; base44 references cleaned from project config files</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` completes without errors
- [ ] `dist/index.html` asset paths are relative (`./assets/...`)
- [ ] `package.json` name is `movingcap-start`
- [ ] No `<link rel="manifest">` in source `index.html`
- [ ] `VITE_BASE_URL` env var is respected in vite.config.js
- [ ] Dev server (`npm run dev`) still works
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Build output deployable from any folder with relative paths
- No base44 references in project configuration
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-pwa-deployment/03-01-SUMMARY.md`
</output>
