---
phase: 01-platform-decoupling
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/api/base44Client.js
  - src/api/entities.js
  - src/api/integrations.js
  - src/lib/AuthContext.jsx
  - src/lib/NavigationTracker.jsx
  - src/lib/VisualEditAgent.jsx
  - src/lib/app-params.js
  - src/lib/query-client.js
  - src/lib/PageNotFound.jsx
  - src/components/UserNotRegisteredError.jsx
  - src/pages.config.js
autonomous: true
requirements: [PLAT-02, PLAT-03, PLAT-04, PLAT-05]
user_setup: []

must_haves:
  truths:
    - "No files exist in src/api/ directory"
    - "src/lib/ contains only utils.js (and use-mobile.jsx in hooks/)"
    - "No source file imports AuthContext, NavigationTracker, VisualEditAgent, app-params, query-client, base44Client, entities, or integrations"
    - "src/pages.config.js does not exist"
    - "src/components/UserNotRegisteredError.jsx does not exist"
    - "src/lib/PageNotFound.jsx does not exist"
  artifacts: []
  key_links:
    - "All deleted files were only imported by App.jsx (rewritten in Plan 01) or by each other — no page component depends on them"
---

<objective>
Delete all Base44 platform files and infrastructure modules that are no longer imported after App.jsx was rewritten in Plan 01.

Purpose: These files contain dead code that references `@base44/sdk` and platform APIs. Leaving them creates confusion and risks accidental re-import. Clean deletion ensures the codebase has zero platform coupling.

Output: Entire `src/api/` directory deleted. Platform files in `src/lib/` deleted. `pages.config.js` and `UserNotRegisteredError.jsx` deleted.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-decoupling/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/api/ directory and platform lib files</name>
  <files>
    src/api/base44Client.js,
    src/api/entities.js,
    src/api/integrations.js,
    src/lib/AuthContext.jsx,
    src/lib/NavigationTracker.jsx,
    src/lib/VisualEditAgent.jsx,
    src/lib/app-params.js,
    src/lib/query-client.js,
    src/lib/PageNotFound.jsx
  </files>
  <action>
Delete the following files using terminal commands:

```bash
# Delete entire api directory
rm -rf src/api/

# Delete platform-specific lib files
rm src/lib/AuthContext.jsx
rm src/lib/NavigationTracker.jsx
rm src/lib/VisualEditAgent.jsx
rm src/lib/app-params.js
rm src/lib/query-client.js
rm src/lib/PageNotFound.jsx
```

**Files remaining in src/lib/ after deletion:** `utils.js` only (the `cn()` helper used by shadcn/ui).

**Why each file is safe to delete:**
- `base44Client.js` — SDK client, only imported by AuthContext, NavigationTracker, PageNotFound (all being deleted)
- `entities.js` — SDK entities, not imported by any page component
- `integrations.js` — SDK integrations (LLM, email, SMS), not imported by any page component
- `AuthContext.jsx` — Was only imported by App.jsx (rewritten) and NavigationTracker (being deleted)
- `NavigationTracker.jsx` — Was only imported by App.jsx (rewritten)
- `VisualEditAgent.jsx` — Was only imported by App.jsx (rewritten)
- `app-params.js` — Was only imported by base44Client.js (being deleted) and AuthContext (being deleted)
- `query-client.js` — Was only imported by App.jsx (rewritten)
- `PageNotFound.jsx` — Replaced by `src/pages/NotFound.jsx` (created in Plan 01)
  </action>
  <verify>
Run: `ls src/api/ 2>/dev/null || echo "api dir deleted"` — should say "api dir deleted".
Run: `ls src/lib/` — should show only `utils.js`.
Run: `npm run build` — should still succeed (App.jsx no longer imports these files).
  </verify>
  <done>src/api/ directory is gone. Platform files in src/lib/ are gone. Only utils.js remains in src/lib/.</done>
</task>

<task type="auto">
  <name>Task 2: Delete pages.config.js and UserNotRegisteredError.jsx</name>
  <files>
    src/pages.config.js,
    src/components/UserNotRegisteredError.jsx
  </files>
  <action>
Delete these remaining platform files:

```bash
rm src/pages.config.js
rm src/components/UserNotRegisteredError.jsx
```

**Why safe to delete:**
- `pages.config.js` — Was only imported by App.jsx (rewritten with explicit routes) and NavigationTracker (deleted in Task 1). Auto-generated routing config for Base44 platform.
- `UserNotRegisteredError.jsx` — Was only imported by App.jsx (rewritten). Shows Base44-specific registration error.

**After deletion, verify no stale imports remain:**
```bash
grep -r "pages.config\|UserNotRegisteredError\|PageNotFound.*lib" src/ --include="*.jsx" --include="*.js" --include="*.ts"
```
Should return zero matches.
  </action>
  <verify>
Run: `grep -rn "pages.config\|UserNotRegisteredError\|PageNotFound.*lib" src/ --include="*.jsx" --include="*.js" --include="*.ts"` — zero matches.
Run: `npm run build` — succeeds.
  </verify>
  <done>pages.config.js and UserNotRegisteredError.jsx deleted. No stale imports anywhere in src/.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/api/` directory does not exist
- [ ] `src/lib/` contains only `utils.js`
- [ ] `src/pages.config.js` does not exist
- [ ] `src/components/UserNotRegisteredError.jsx` does not exist
- [ ] `grep -r "@base44\|base44Client\|base44\.auth\|base44\.appLogs" src/` returns zero matches
- [ ] `grep -r "AuthContext\|NavigationTracker\|VisualEditAgent\|app-params\|query-client" src/` returns zero matches
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All platform files deleted
- No stale imports remain in any source file
- Build still succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-decoupling/01-02-SUMMARY.md`
</output>
